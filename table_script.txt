CUSTOMER 

CUST_ID 		INT AUTO_INCREMENT PRIMARY KEY
F_NAME   		VARCHAR
L_NAME 			VARCHAR
DOB				VARCHAR 
EMAIL			VARCHAR
MOBILE			VARCHAR
REF_FROM		INT
REF_TO			VARCHAR
MARITAL_STATUS	VARCHAR
DOA				VARCHAR
SMS_ENABLED		TINYIST(1)
IS_EMABLED		TINYIST(1)
CREATED_AT		TIMESTAMP
UPDATED_AT		TIMESTAMP

SERVICES

SERVICE_ID		INT AUTO_INCREMENT PRIMARY KEY
SERVICE_NAME	VARCHAR
IS_ACTIVE		TINYIST(1)
CREATED_AT		TIMESTAMP
UPDATED_AT		TIMESTAMP	

SERVICE_USAGE

USAGE_ID			INT AUTO_INCREMENT PRIMARY KEY
CUST_ID				INT
SERVICE_ID			INT
USAGE_DATE			DATA
STATUS				VARCHAR
REMARKS				VARCHAR
CREATED_AT			TIMESTAMP
UPDATED_AT			TIMESTAMP


give me sql to create tables in mysql CUSTOMER ,SERVICES and SERVICE_USAGE with following details 

CUST_ID 		INT AUTO_INCREMENT PRIMARY KEY
F_NAME   		VARCHAR
L_NAME 			VARCHAR
DOB				VARCHAR 
EMAIL			VARCHAR
MOBILE			VARCHAR
REF_FROM		INT
REF_TO			VARCHAR
MARITAL_STATUS	VARCHAR
DOA				VARCHAR
SMS_ENABLED		TINYIST(1)
IS_EMABLED		TINYIST(1)
CREATED_AT		TIMESTAMP
UPDATED_AT		TIMESTAMP

SERVICES

SERVICE_ID		INT AUTO_INCREMENT PRIMARY KEY
SERVICE_NAME	VARCHAR
IS_ACTIVE		TINYIST(1)
CREATED_AT		TIMESTAMP
UPDATED_AT		TIMESTAMP	

SERVICE_USAGE

USAGE_ID			INT AUTO_INCREMENT PRIMARY KEY
CUST_ID				INT
SERVICE_ID			INT
USAGE_DATE			DATA
STATUS				VARCHAR
REMARKS				VARCHAR
CREATED_AT			TIMESTAMP
UPDATED_AT			TIMESTAMP


CREATE TABLE CUSTOMER (
    CUST_ID INT AUTO_INCREMENT PRIMARY KEY,
    F_NAME VARCHAR(100),
    L_NAME VARCHAR(100),
    DOB VARCHAR(50),
    EMAIL VARCHAR(100),
    MOBILE VARCHAR(20),
    REF_FROM INT,
    REF_TO VARCHAR(100),
    MARITAL_STATUS VARCHAR(50),
    DOA VARCHAR(50),
    SMS_ENABLED TINYINT(1),
    IS_ENABLED TINYINT(1),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Create SERVICES table
CREATE TABLE SERVICES (
    SERVICE_ID INT AUTO_INCREMENT PRIMARY KEY,
    SERVICE_NAME VARCHAR(100),
    IS_ACTIVE TINYINT(1),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Create SERVICE_USAGE table
CREATE TABLE SERVICE_USAGE (
    USAGE_ID INT AUTO_INCREMENT PRIMARY KEY,
    CUST_ID INT,
    SERVICE_ID INT,
    USAGE_DATE DATE,
    STATUS VARCHAR(50),
    REMARKS VARCHAR(255),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (CUST_ID) REFERENCES CUSTOMER(CUST_ID),
    FOREIGN KEY (SERVICE_ID) REFERENCES SERVICES(SERVICE_ID)
);